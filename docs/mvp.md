# Chat Hub — MVP checklist

- [ ] Electron + React + TS scaffold
- [ ] Sidebar session list + new session
- [ ] Chat transcript UI (user/assistant)
- [ ] SessionManager + mock adapter (echo / fake stream)
- [ ] Status from events only
- [ ] OS notification if session → waiting_input / done (without Monitor)
- [ ] One real adapter (Grok **or** OpenCode)
- [ ] Optional: write events to local socket/file for Session Monitor
- [ ] Persist sessions list + last messages

Out of MVP: full tool UI parity, plan review, all 4 providers, worktree manager.
